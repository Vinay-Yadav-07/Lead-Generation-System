ICP Config
     ->
Discovery Engine
     ->
Enrichment Engine
     ->
Verification Engine
     ->
Confidence Scoring
     ->
SQLite Database
     ->
REST API
     ->
Dashboard

Implemented modules:

1. FastAPI backend in `main.py`
2. SQLite models in `models/`
3. Discovery, enrichment, verification, and outreach services in `services/`
4. Dashboard static frontend in `static/`
5. Configurable ICP in `config/icp.json`
6. CSV/JSON exports and outreach tracking
